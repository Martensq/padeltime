{% extends 'themes/base_admin.html.twig' %}

{% block title %}Liste des utilisateurs{% endblock %}

{% block description %}Liste des utilisateurs{% endblock %}

{% block keywords %}Liste, utilisateurs, admin{% endblock %}

{% block main %}
    <h1 class="text-center my-3 display-5">Liste des utilisateurs</h1>

    {% include 'components/_alert.html.twig' %}

    <div class="table-responsive">
        <table class="table table-striped table-hover text-center table-bordered">
            <thead class="table-dark">
                <tr>
                    <th>Id</th>
                    <th>Nom</th>
                    <th>Prénom</th>
                    <th>Email</th>
                    <th>Role</th>
                    <th>Crée le</th>
                    <th>Modifié le</th>
                    <th>Options</th>
                </tr>
            </thead>
            {# <tbody>
                {% for court in courts %}
                    <tr>
                        <td>{{court.id}}</td>
                        <td>Piste {{court.courtNumber}}</td>
                        <td class="h-100 d-flex flex-column justify-content-center align-items-center">
                            {% if court.isAvailable %}
                                <span>Disponible</span>
                            {% else %}
                                <span>Indisponible</span>
                            {% endif %}
                            <form action="{{path('admin_court_available', {'id': court.id})}}" method="post">
                                <div class="form-check form-switch">
                                    <input onclick="return confirm('Confirmer cette action ?')" onchange="this.form.submit()" class="form-check-input" type="checkbox" role="switch" {{court.isAvailable ? 'checked' : ''}}>
                                </div>
                            </form>
                        </td>
                        <td>{{court.createdAt|format_date()}}</td>
                        <td>{{court.updatedAt|format_date()}}</td>
                        <td>
                            <a href="{{path('admin_court_edit', {'id': court.id})}}" class="btn btn-sm btn-secondary">Modifier</a>
                            <form class="d-inline" action="{{path('admin_court_delete', {'id': court.id})}}" method="post">
                                <input type="hidden" name="_csrf_token" value="{{csrf_token('delete_court_'~court.id)}}">
                                <input onclick="return confirm('Confirmer la suppression de la piste ?')" type="submit" class="btn btn-sm btn-danger" value="Supprimer">
                            </form>
                        </td>
                    </tr>
                {% endfor %}
            </body> #}
        </table>
    </div>

{% endblock %}